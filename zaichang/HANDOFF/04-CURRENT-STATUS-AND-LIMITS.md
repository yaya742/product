# 当前状态与限制

快照标识：20260915-1037（Asia/Shanghai）；版本 0.3.0；HEAD `08f95f633536fb119c5dd7c9206ccd3aa22ab81b`；提交数 **1**。原工作树包含 260 项修改、2 项删除、6592 项未跟踪状态记录（含大量不分发的产物），不是干净提交。逐文件来源和哈希见 SOURCE-MANIFEST，采集时间范围和最终不变性检查见 VERIFICATION。

## 本次干净解压结果

依赖与短路径Hermes安装、构建、类型检查通过，地图核心22/22、Luna合成机制5/5通过。核心15/26通过（10失败、1取消），后端211/251通过（40失败）；Hermes导入遇到读取边界PermissionError，图文发送和部分Electron控件/撤销等待也未通过。完整地图测试约290秒未完成，单独的无录像地图视图可打开并截图。没有修产品源码；本包以“验证失败的当前源码快照”交付。

## 证据必须分层

| 证据类型 | 重新核实的已有结果 | 限制 |
|---|---|---|
| 确定性机制 | `luna-adapter-backend-gate/tests.json` 于 2026-09-13 22:35（上海）结束，23 套249/249；随后 long-input-review 专项2/2、Hermes身份9/9、隐私10/10 | 完整门禁早于部分新源码；本次干净解压运行结果另列 VERIFICATION，不用旧数字代替 |
| DeepSeek 真实开发 | 原60故事×3有180条，171通过、8任务失败、1未完成判分；其后针对性修复单列；较新 candidate 仅2/180后遇到402 | 历史余额状态没有在本次查询；新模型/新代码不能继承该成绩 |
| Luna 真实开发 | `luna-sixty-stories-v2/report.json` 2026-09-14 00:32结束，**180/180 条、159通过、21未通过、1072次调用** | 临时代测品；使用隔离合成业务数据与模型评分，无独立人评；状态为 incomplete_or_failed，而非全部通过 |
| Luna 定向补证 | 较新 S30 委派3/3；长文本原7条仅2通过，随后 L01/L07 pilot 2/2；B0/B1/B2/B3六题对照分别6/6、4/6、6/6、6/6 | 不把选中的修复样本拼成完整180条全绿；B1未办理本地事项的旧退化保留 |
| Electron/图文 | DeepSeek 2026-09-12 与 Luna 2026-09-13 各有文本段1、JPEG段1的真实模型链记录；本包可选图片只来自本次重新运行的合成 Electron | 旧报告不是本次快照普遍视觉正确率；现有界面仍可能将 Luna 显示为 DeepSeek |
| 外部服务 | 代码包含可选校园读取器、城市天气和系统位置适配 | 本次未连接真实账户/模型/定位或执行外部写入；接口不证明服务已接通 |
| 人工评审 | 未找到已完成的独立用户/读者评审或真正隔离盲测证明 | 主代理复核、模型评分和截图不能替代独立人评 |

Luna 的21条未通过记录涉及：S04:r2、S05:r2、S08:r2/r3、S12:r1/r2、S21:r1/r2/r3、S25:r1/r2/r3、S35:r1/r2/r3、N04:r2、N12:r3、N15:r1、N19:r1/r2/r3。失败包含调用故障、遗漏、控制/动作处理与评测误判，不能全部归咎模型，也不能全部算作已修复。S05:r2的简短答复后来原轨迹复判通过；较早 S01 时区误判有独立复判。原始分数仍保留，此包只收统计，不收模型原文或工具正文。

## 源码和旧文档之间的冲突

- `UNDERSTANDING-ACTION-IMPLEMENTATION` 的基线段仍说 Hermes 未接入；当前 `harness.ts → runtime/hermes.ts → sidecar.py` 已实际调用固定 Hermes。该段只能当历史基线。
- README 中“临时模式不保存新安排”比当前实现窄：`localEffectScope` 允许在独立验证明确本地委托后持久化必要业务结果，同时不保存聊天原文。仍需完整任务质量复验。
- PROJECT-NAVIGATION 同时保留217、248、249等不同轮次数字、过时的 V65/S30待修段。CURRENT-REPORT 的生成时间早于最终 Luna180条；active-runs 也是旧时间的状态文件。不能只读它们的“最新”措辞。
- 当前源码有长原话的额外收尾核验；较早完整批次编译版本没有这项补丁。没有一次最新源码完整真实模型复验可宣称全部问题关闭。
- Luna 适配没有与 DeepSeek 完全相同的协议能力：服务端拒绝输出 token 上限，宿主只能完成后拒收；账户费用未知；当前 HTTP 503直接报错，未实现与 DeepSeek相同的那次自动重试。这里只记录，不修改。
- 旧截图/便携包、minimal-core 审查切片和文档中“本轮完成”不等于这份最新源码已产品验收。

## 下一位同学最需要知道的未完成项

理解与行动后端仍有未闭合真实模型失败；新前端阶段和正式成品体验未完成，Luna代测标识需要完善。真正盲测、独立人评、手机驻留/跨设备、真实外部发送/预约与后台必达通知均未完成。本次任务只冻结、解释和验证源码，未继续产品目标、未修业务缺陷、未启用两个前端设计技能。
