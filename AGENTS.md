# AGENT.md

## 注意事项

1. 每次写完代码后，先自行检查代码是否正常，再回复用户。
2. 每次修改完成后执行 `git commit`，以便后续追踪和回退。
3. 对没有把握的事项先与用户商量，不要擅自做决定。

---

# 在场（Zaichang）项目规划与开发协作基线

> 本文件是当前项目的产品规划、技术路线、数据边界和发布规则。根目录 `AGENT.md` 中的通用协作约束继续有效；本文件重点补充“项目应该做什么、先做什么，以及如何把代码放到正确的 GitHub 分支”。

## 1. 项目定位

在场的目标不是把校园网站简单套上一层聊天框，而是帮助学生处理“眼前的校园事务和下一步行动”：先取得有来源的数据，再由 Agent 解释、组合和提醒；涉及写入、发送、预约、支付或其他不可逆动作时，必须由用户明确确认并返回实际回执。

第一阶段聚焦浙江大学学生的学习与校园生活场景：

- 公开校园知识：校区、建筑、课程说明、培养方案、校园服务入口、公开通知和办事路径。
- 本人学业资料：课表、课程、考试、作业/学习平台课程和其他已明确接入的只读领域。
- 行动规划：把课程、考试、地点、天气、路程和本地任务组合成时间线、提醒和下一步建议。
- 本地 Agent：桌面端使用 Electron/React 和本地运行时；移动端保持手机本地运行，直接调用用户配置的 DeepSeek API，不依赖用户电脑或额外云端网关。

不把“接入越多数据”当作产品目标。优先证明少数真实场景可靠，再扩展到更多校园系统。

## 2. 当前仓库状态与基线选择

截至 2026-09-22，当前 checkout 是 `main`，`origin/main` 的跟踪内容只有根目录 `AGENT.md`。本地的 `zaichang/`、`apkceshi/`、`weather_plugin/`、`tests/` 是未跟踪目录，其中混有 APK、构建输出、运行时目录、缓存和测试数据库，不能直接整体 `git add .`。

远端已有的职责边界如下，后续开发应以远端源代码分支或隔离 worktree 为准：

- `origin/codex/zaichang-desktop`：桌面 Electron/React、Agent Harness、插件宿主、地图和校园连接器。
- `origin/mobile_app`：Android/Capacitor 移动端及手机侧能力。
- `origin/school`：学校插件和校园信息扩展。
- `origin/weather`：天气服务/插件。
- `origin/codex/reorg-staging`：较完整的阶段性整合候选，适合在核对差异后作为集成基线。
- `origin/main`：稳定发布入口；在没有完成源码归位、测试和审查前，不应把它当作产品源码分支。

第一项实际工作不是继续扩大功能，而是完成一次源码归位：选定一个明确的集成基线，分别确认桌面、移动端、学校插件和天气插件的源代码、锁文件、测试、文档和忽略规则；把 `node_modules`、`dist`、`release`、`artifacts`、`.runtime`、`.dev-data`、`.test-data`、APK 成品和 `__pycache__` 等生成物排除在源码提交之外。

## 3. 外部资料带来的设计结论

### 3.1 Pcelechron：产品和跨平台工程参考

参考资料：

- [Pcelechron Releases](https://github.com/Flaviohor/Pcelechron/releases)
- [Pcelechron README](https://github.com/Flaviohor/Pcelechron/blob/main/README.md)
- [Windows 移植说明](https://raw.githubusercontent.com/Flaviohor/Pcelechron/main/WINDOWS_PORT.md)
- [Pcelechron pubspec.yaml](https://raw.githubusercontent.com/Flaviohor/Pcelechron/main/pubspec.yaml)

从 Pcelechron 及其发布说明中吸收以下经验：

1. 产品价值应从高频闭环开始：日程一览、课表、DDL、成绩/学业信息和提醒比“万能问答”更容易验证。
2. 跨平台代码要把平台差异隔离在适配层。例如后台刷新、通知、深链、系统日历、密钥存储和窗口/安装器能力不能散落在业务页面中。
3. 数据迁移和标识符稳定性必须在第一版设计。应用显示名、安装目录、数据目录、深链协议、Android applicationId 和 iOS bundle id 不能为了改名随意改变，否则会造成登录状态或缓存丢失。
4. 发布是产品的一部分：安装版、便携版、不同 CPU 架构、运行库、系统要求、SmartScreen/签名、数据保留和卸载行为都应成为验收项。
5. Pcelechron 仓库采用 GPLv3。当前项目默认只借鉴产品闭环、适配层思路和发布经验，不直接复制其代码；如未来确需复用代码，必须先做许可证兼容审查、保留版权与许可证，并将复用范围记录在 `THIRD_PARTY_NOTICES.md`。

Pcelechron 是参考客户端，不是浙江大学官方个人数据 API。它可以帮助我们设计“课程/日程/提醒”的用户体验和跨平台工程，但不能替代对真实校园来源、授权方式、字段含义和失败状态的核验。

### 3.2 ZJUers：公开校园知识和入口目录参考

参考资料：

- [ZJUers 轻首页](https://zjuers.com/)
- [2026 年浙江大学本科新生指引](https://zjuers.com/welcome/)
- [基本概念与四课融通](https://zjuers.com/welcome/learning/concepts/)
- [培养方案](https://zjuers.com/welcome/learning/program/)
- [校园区域](https://zjuers.com/welcome/life/campus/)

ZJUers 首页把新生指引、教学、教务、RVPN/WebVPN、图书馆、教师信息查询、素质拓展和其他校园服务入口集中起来；新生指引还提供校园区域、课程、培养方案和第二/第三/第四课堂等解释。这些内容适合作为“公开资料层”和链接导航层：

- 公开资料可以直接服务未登录用户，回答“这个地方/课程/流程是什么、去哪里办理、原文在哪里”。
- 页面内容要保存来源 URL、抓取时间、内容版本或摘要，并在答案中允许用户回到原文；不能只保留模型改写后的结论。
- ZJUers 的指南和社区整理不能被当成校方实时状态、个人成绩或个人课表；关键事实需要校方公开页面或本人授权连接器再次核对。
- 对动态页面、登录后页面、需要 VPN/CAS 的页面，先提供原始入口和“需要登录/网络条件”的状态，不用猜测缺失内容。
- 对课程、培养方案和第二/第三/第四课堂等内容，应采用“知识说明 + 原文链接 + 更新时间”的形式，避免把指南中的建议误标成强制校规。

## 4. Agent + 插件总体架构

项目的核心架构明确为 **Agent + Plugins**：Agent 是通用的理解、决策和编排核心；课程、校园公开资料、个人课表、考试、天气、地图、定位、日程、通知等具体功能全部通过插件提供。即使某个插件随安装包内置，也必须遵守和第三方插件相同的能力契约，不能把业务逻辑重新写死在 Agent 核心里。

Agent 核心只负责：

- 理解用户意图，判断是查询、解释、规划、提醒还是执行请求。
- 读取当前会话允许使用的最小上下文和用户授权状态。
- 根据能力清单、权限、平台和数据新鲜度选择插件并编排调用顺序。
- 校验插件结果的 schema、来源、覆盖范围、时效和冲突状态。
- 将结构化事实组织成回答、计划或待确认动作。
- 管理确认、取消、重试、幂等、回执、审计和错误恢复。

Agent 核心不负责：

- 直接写 ZJUers、校方或其他校园系统的 HTTP 请求。
- 解析某一个校园网站的 HTML、验证码、Cookie 或私有字段。
- 把课程、天气、地图、通知等具体领域规则散落在提示词和 UI 页面里。
- 越过插件声明的权限向模型提供原始个人数据。

插件负责一个清晰、可测试、可替换的能力边界。例如：

- `campus-public-zjuers`：公开校园指南、校区/建筑、课程概念和服务入口，带原文链接和更新时间。
- `campus-personal-zju`：本人授权的课表、课程、考试和学习平台资料，首阶段只读。
- `weather`：当前位置或校园默认位置的天气和户外活动评估。
- `campus-map` / `location`：校园地点、路线和设备定位。
- `local-calendar` / `notification`：本地日程、复习任务和系统提醒。
- 后续的 `grades`、`sports`、`library`、`campus-notices`、`mail` 等也必须独立成插件，不能因为属于同一个产品就直接进入 Agent 核心。

```text
用户请求
   │
   ▼
Agent Core
   ├─ 意图与上下文
   ├─ 权限/策略/确认
   ├─ Plugin Registry
   ├─ 调用编排与结果核验
   └─ 回复、计划、回执与记忆
        │ 只通过统一协议调用
        ▼
Plugin Runtime
   ├─ campus-public-zjuers
   ├─ campus-personal-zju
   ├─ course / schedule / exam
   ├─ weather / campus-map / location
   └─ local-calendar / notification / future plugins
        │
        ├─ 桌面端：Electron/React + 本地宿主 + 隔离插件进程
        └─ 移动端：Capacitor/Android + 本机插件适配层 + 直接 DeepSeek
```

### 4.1 Agent 核心与插件运行层的边界

插件由清单注册，由宿主或移动端插件运行层加载。内置插件和用户安装的第三方插件走同一套 manifest、schema、权限、超时、网络域名和生命周期检查；区别只在于来源、信任等级和审核状态，不在于是否可以绕过安全边界。

插件运行层至少需要支持：

- 安装、升级、启用、停用、卸载和版本回滚。
- 每个插件的能力预览、所需 scope、出站域名、平台支持、数据保留策略和许可证。
- 桌面端独立进程或等价隔离；移动端最小权限的原生适配层，不能让插件任意访问 WebView 或其他插件数据。
- 请求超时、最大响应大小、取消信号、资源限制、审计事件和错误映射。
- 只读、本地写入、外部写入三级 effect；外部写入必须经过用户确认并检查真实回执。
- 插件不可用时的明确状态和替代路径，而不是由 Agent 猜测或偷偷调用内置后门。

Agent 通过 `Plugin Registry` 发现能力，不通过文件路径、特定模块名或隐含的内置分支发现能力。这样可以让地图、天气、校园连接器先在桌面端成熟，再逐步提供移动端实现，同时保持 Agent 的调用协议不变。

### 4.2 插件/连接器统一契约

每个能力都应有 manifest 和稳定版本，至少声明：能力名、版本、输入 schema、输出 schema、数据来源、所需 scope、只读/本地写入/外部写入、网络域名、超时、最大返回量和平台支持。

每次返回都要尽量包含：

- `status`：`ok`、`partial`、`stale`、`auth_required`、`unavailable` 或 `error`。
- `origin`/`source_id`：来自公开页面、本人账号、设备、缓存还是用户手动输入。
- `fetched_at`/`evaluated_at`：数据取得或规则计算时间。
- `coverage`：覆盖了哪些学期、日期、课程或数据域，是否完整。
- `freshness`：是否超过缓存策略；刷新失败时不得把旧资料伪装成最新资料。
- `next_step`：用户需要登录、开启 VPN、重新授权、手动确认或打开原文时给出明确入口。

插件失败时保留上次可用缓存并标记过期/部分结果；网络超时不能翻译成“没有记录”。模型只能解释结构化结果，不能补全缺失的课程时间、考试地点、成绩或校方规则。

### 4.3 公共资料和个人资料必须分开

公开资料层可以默认启用、无账号读取；个人资料层必须按领域单独授权。模型只接收完成当前问题所需的最小摘要，不接收密码、Cookie、完整学号、校园卡号、身份证号、原始认证响应或未筛选的成绩/个人档案。

个人连接器第一阶段保持只读：课表、课程、考试、学习平台课程和已验证的待办可以进入 Agent；成绩、绩点、校园卡、预约、签到、支付和修改个人资料要单独标为未接入或待确认，不得因为页面上存在入口就宣称已经支持。

### 4.4 数据和存储

分离三类数据：

1. 原始响应：只在连接器进程或短期受控缓存中使用，默认不进入模型和长期数据库。
2. 标准化事实：课程、时间段、考试、地点、公开资料条目，保留来源、时间和覆盖范围。
3. 用户记忆/计划：偏好、目标、已确认本地安排和反馈；可查看、修改、停用、删除，不能取代真实校园事实。

桌面端敏感令牌使用 Windows 当前用户的 DPAPI/safeStorage；移动端使用 Android Keystore/系统安全存储。禁止把校园密码、学生 ID 或 DeepSeek API Key 放进 `localStorage`、普通日志、崩溃报告、导出文件或 Git。任何迁移都必须包括旧数据清理、删除验证、备份边界和回滚说明。

## 5. 产品范围和实现顺序

### 阶段 0：源码归位和契约冻结

目标：先获得可重复构建的唯一源码基线。

- 从 `origin/codex/reorg-staging`、`origin/codex/zaichang-desktop`、`origin/mobile_app`、`origin/school`、`origin/weather` 分别核对提交、文件树和职责，不把当前未跟踪运行时目录当作源代码。
- 冻结 `shared` 的领域 schema、状态枚举、来源/新鲜度字段和插件 manifest。
- 增加 `.gitignore`、依赖锁文件、构建入口、源码/生成物说明和第三方许可证清单。
- 将桌面、移动端、插件和测试放到明确的责任目录，必要时使用独立 worktree 完成迁移。

验收：在干净 worktree 中可以执行静态检查；文档能明确说明每个目录的来源、所有者和构建方式；没有密钥、APK 或运行时数据库被纳入源代码提交。

### 阶段 1：公开校园知识 MVP

目标：先服务不需要账号的真实问题。

- 建立 `campus.public` 只读能力，首批覆盖 ZJUers 的校区/建筑、课程概念、培养方案、课堂体系和校园服务入口。
- 每个知识条目保存标题、摘要、原文 URL、来源类型、抓取时间、有效期和人工核验状态。
- 对动态或可能过期内容显示“来源/更新时间/需要进一步核对”，提供打开原文按钮。
- 搜索和模型回答都只引用已抓取的公开条目，禁止将个人数据和公开数据混成同一条事实。

验收场景：用户问“第二课堂是什么”“某校区/建筑在哪里”“教务/图书馆/RVPN 从哪里进入”，回答包含可回链来源，不需要登录，也不会把社区指南冒充官方通知。

### 阶段 2：个人学业资料只读接入

目标：形成“公开信息 + 本人课表/考试”的可靠组合。

- 复用现有 `zju-student-info` 的连接器边界，但把认证、缓存、标准化和 Agent 摘要分层。
- 连接时明确展示学校、数据域、最近成功同步时间和失败原因；支持按领域刷新和撤销。
- 先做课表/课程/考试/学习平台课程；校历、调停课和具体日期必须有独立来源或明确不确定性，不能从星期和节次猜出确定日期。
- 将 Pcelechron 体现的课表/日程/DDL 用户闭环转化为本项目的标准数据模型，不直接拷贝其实现。

验收场景：用户问“我明天有什么课”“这门课的学习任务是什么”“考试在哪儿”，系统只返回连接器实际覆盖的内容；无账号、CAS/VPN 失败、验证码或接口变更时，返回 `auth_required`/`partial`/`unavailable`，不返回编造的空结果。

### 阶段 3：行动建议和本地提醒

目标：把事实变成用户可以执行的下一步。

- 课程/考试与校园地图、GPS、天气组合，生成出发时间、路线和风险提示。
- 本地日程、复习任务和提醒可以写入，但要区分“建议”“已登记”“已执行”“未知回执”。
- 所有外部写入、预约、发送、支付或不可逆操作必须二次确认；确认后仍需检查真实回执。
- 提醒支持优先级、免打扰时间、重复策略、撤销入口和失败重试；不承诺应用被关闭后一定送达。

核心演示：

> “我明天有物理考试，查考场，结合我的位置和天气规划路线，告诉我几点出发，并在本地建立一个提醒。”

验收必须分别证明：考试事实来自个人连接器、地点来自可追溯地图资料、天气标注时效、提醒确实写入本地系统；不能用一条模型回复代替四项证据。

### 阶段 4：移动端闭环和跨平台发布

目标：手机在没有用户电脑和云端网关的情况下也能完成基础对话和本地能力。

- 移动端优先支持公开资料、已缓存的标准化学业资料、本地日程、通知、位置和天气；个人凭据放入 Keystore，不写入 WebView `localStorage`。
- 把核心 schema、规则计算和错误状态做成共享契约；平台 API（通知、定位、后台任务、安全存储）由 Android 适配层实现。
- 借鉴 Pcelechron 的平台适配拆分：后台刷新、通知和系统日历不能写死在业务页面；桌面和移动端使用各自调度器，但共用刷新任务、去重和前台让行规则。
- 发布前分别完成 Web/Vite 构建、Capacitor sync、Gradle APK、安装启动、真实设备和真实账号验收；一个阶段通过不代表其他阶段通过。

### 阶段 5：扩展和公开发布

在前述闭环稳定后再考虑成绩/绩点、体育打卡、校园通知、图书馆预约、邮箱、钉钉、健康或多学校适配。高敏感域必须单独做威胁建模、授权、审计、撤销和删除测试，不能因为存在网页入口就自动接入。

## 6. 验证和发布门槛

所有报告必须区分以下证据：

- 代码/契约：TypeScript、Python、schema、插件 manifest 和静态安全检查。
- 离线行为：fixture、故障注入、缓存过期、权限拒绝、数据删除和迁移测试。
- 构建/打包：桌面 build、APK build、安装器/便携版产物和 checksum。
- 运行时：Electron/Android 启动、窗口响应、通知、地图和本地存储。
- 真实验收：真实 ZJU 账号、RVPN/VPN、真实设备、真实模型额度和真实校园网络。

真实账号/设备没有运行时，不能写成“已完成”；网络超时不能写成“没有数据”；浏览器能打开页面不能写成“连接器已支持”。

每个阶段至少准备：

- 正常路径：公开资料命中、个人资料命中、本地提醒成功。
- 缺失路径：未授权、过期、部分领域缺失、原文失效、网络不可达、验证码或服务端变更。
- 安全路径：日志/错误/导出/模型上下文不泄露密码、Cookie、完整学号和校园卡号；删除后旧缓存不可再次被模型读取。
- 发布路径：干净 worktree 安装依赖、构建、启动、升级和卸载；升级后数据目录、深链和设置仍然可用。

## 7. Git 分支与 GitHub 上传规则

这条规则是项目硬约束：**向 GitHub 上传代码前，必须先把代码放在与功能职责匹配的分支中；禁止把本地脏 checkout、构建目录或临时实验直接上传到 `main`。**

### 7.1 分支职责

- `main`：稳定、可发布、经过合并和验收的基线，只通过 Pull Request 合入。
- 桌面功能：继续使用现有 `codex/zaichang-desktop` 责任线；新功能从正确基线创建 `codex/desktop-<topic>`。
- 移动功能：现有移动责任线为 `mobile_app`；新功能使用 `codex/mobile-<topic>`，完成后再合入移动责任线或集成分支。
- 学校插件：现有责任线为 `school`；新连接器使用 `codex/school-<topic>`。
- 天气/地图等独立能力：使用对应责任分支；跨端组合功能使用 `codex/integration-<milestone>`。
- 发布准备：使用 `codex/release-<version>`，只放版本号、迁移、打包、安装器、发布说明和验收修复，不在其中堆积未审查的新功能。

已有分支名称不完全统一时，先核对远端提交和 worktree，不为了“看起来统一”擅自重写历史或删除分支；从现在开始新建分支使用 `codex/` 前缀，并在 PR 中说明最终合入目标。

### 7.2 每次上传前的固定步骤

1. `git fetch origin --prune`，确认基线、远端分支和是否有同名更新。
2. 检查 `git status --short --branch`、`git worktree list` 和变更文件清单；脏 checkout 需要用隔离 worktree，不把无关改动带进提交。
3. 从正确的基线创建或切换到职责分支，只修改该功能范围。
4. 运行与范围相符的静态检查、单元/契约测试和构建；真实账号/设备验证单独记录，不用离线测试冒充。
5. 审查 `git diff --cached --name-status`：确认没有密钥、Cookie、学生完整身份信息、`node_modules`、`dist`、`release`、`artifacts`、APK、数据库、`.pyc`、`.runtime` 或临时截图。
6. 用一个可回退的提交记录一个逻辑变更，提交信息说明功能和验证范围。
7. `git push -u origin <branch>` 上传责任分支；推送后用 `git ls-remote --heads origin <branch>` 核对远端 ref 和提交 SHA。
8. 创建或更新 PR，目标是对应的责任线/集成分支；CI、人工审查和必要的真实验收通过后，才允许合入 `main`。
9. 发布版使用 tag 和 GitHub Release；安装器、便携版或 APK 作为经过核对的 Release asset，不把生成物倒灌进源码分支。

禁止的快捷做法：在 `main` 上直接开发并 push、为“备份”把整个工作区压成 ZIP 提交、把本地 APK 当作源码、用 `git add .` 绕过文件审查、在未确认远端状态时强推或重写历史。

## 8. 完成定义

一个功能只有同时满足以下条件，才称为“完成”：

- 源码位于正确责任分支，提交和 PR 可追踪，远端 ref 已核对。
- 输入、输出、权限、来源、新鲜度、失败状态和删除行为有文档及测试。
- 桌面/移动端的相关构建和运行时门槛已经实际执行；未执行的真实账号/设备门槛明确标为未验证。
- UI 不把内部工程术语、模型猜测或状态不确定性伪装成确定事实。
- 日志、模型上下文、导出和崩溃信息没有暴露敏感凭据。
- 公开资料可回链，个人资料可撤销，外部写入有用户确认和真实回执。

## 9. 首个迭代建议

建议按以下顺序落地，不要同时启动所有校园域：

1. 选定 `origin/codex/reorg-staging` 或经过审查的等价提交作为集成基线，完成源码归位和忽略规则。
2. 冻结 `campus.public`、`campus.personal`、`schedule`、`exam`、`place`、`weather` 的 schema 与状态协议。
3. 做 ZJUers 公开资料索引和来源卡片，先覆盖新生指引、校区/建筑、课程概念、培养方案和校园服务入口。
4. 将现有浙大连接器收敛为课表/课程/考试/学习课程的只读摘要，完成凭据迁移和隐私测试。
5. 打通“考试事实 + 地点 + 天气 + 路线 + 本地提醒”的桌面闭环。
6. 再把同一闭环迁移到手机本地，完成 Keystore、通知、网络失败和真实 Android 设备验收。
7. 最后根据真实使用反馈决定是否接入成绩、体育、校园通知或预约。

## 10. 资料说明

本规划依据当前远端分支树、项目已有桌面/移动端结构，以及上面列出的 Pcelechron 和 ZJUers 页面整理。外部页面和校园服务会变化；每次发布前应重新核对版本、链接、接口可用性、许可证、登录流程和数据覆盖范围。
